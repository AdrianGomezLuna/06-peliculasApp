import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.peliculasApp',
  appName: 'peliculasApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
